{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cd TrendWeight && dotnet watch --non-interactive run",
    "build": "dotnet build",
    "test": "dotnet test",
    "format": "dotnet format",
    "format:check": "dotnet format --verify-no-changes",
    "clean": "find . -name 'bin' -type d -prune -exec rm -rf '{}' + && find . -name 'obj' -type d -prune -exec rm -rf '{}' +"
  }
}